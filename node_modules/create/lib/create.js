import { mergeCreations } from "./merge/mergeCreations";
export async function create(preset, options, context) {
    const take = (input, takeOptions) => {
        return input({ ...context, options: takeOptions, take });
    };
    const creations = await preset({ ...context, options, take });
    const creation = mergeCreations(creations);
    return creation;
}
//# sourceMappingURL=create.js.map