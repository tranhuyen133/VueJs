import { execa } from "execa";
import * as nodeFS from "node:fs/promises";
export function setupContext() {
    const fetcher = fetch;
    const fs = {
        readFile: async (filePath) => (await nodeFS.readFile(filePath)).toString(),
        writeFile: async (filePath, contents) => {
            await nodeFS.writeFile(filePath, contents);
        },
    };
    const runner = execa;
    const take = (input, options) => input({ fetcher, fs, options, runner, take });
    return { fetcher, fs, runner, take };
}
//# sourceMappingURL=setupContext.js.map