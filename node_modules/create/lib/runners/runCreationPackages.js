export async function runCreationPackages(packages, context) {
    for (const [key, suffix] of [
        ["dependencies", ""],
        ["devDependencies", " --save-dev"],
        ["peerDependencies", " --save-peer"],
    ]) {
        if (!packages[key]) {
            continue;
        }
        const args = Object.entries(packages[key])
            .map((pair) => pair.join("@"))
            .join(" ");
        await context.runner(`pnpm add ${args}${suffix}`);
    }
}
//# sourceMappingURL=runCreationPackages.js.map