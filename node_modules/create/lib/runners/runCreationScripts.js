export async function runCreationScripts(scripts, context) {
    const packageData = JSON.parse((await context.fs.readFile("package.json")) || "{}");
    await context.fs.writeFile("package.json", JSON.stringify({
        ...packageData,
        scripts: {
            ...packageData.scripts,
            ...scripts,
        },
    }, null, 2));
}
//# sourceMappingURL=runCreationScripts.js.map